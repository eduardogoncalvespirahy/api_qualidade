export interface FormDraft {
  formId: string;
  rascunhoData: string;
}

export interface CreateFormDraftDTO {
  formId: string;
  rascunhoData: string;
}

export interface UpdateFormDraftDTO {
  formId: string;
  rascunhoData: string;
}
