export interface ControlStatus {
  controlId: string;
  statusId: string;
}

export interface CreateControlStatusDTO {
  controlId: string;
  statusId: string;
}

export interface UpdateControlStatusDTO {
  controlId: string;
  statusId: string;
}